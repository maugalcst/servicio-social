import { DashboardHeader } from "@/components/dashboard-header";
import { prisma } from "@/lib/prisma";
export default async function PersonalPage() { const users = await prisma.user.findMany({ include: { career: true }, orderBy: { name: "asc" } }); return <><DashboardHeader title="Personal" subtitle="Administración de usuarios del sistema." /><div className="content-wrap"><section className="table-card"><div className="table-heading"><div><h2>Personal registrado</h2><p>Administradores, coordinadores y docentes</p></div></div><table><thead><tr><th>Nombre</th><th>Correo</th><th>Rol</th><th>Carrera</th><th>Estado</th></tr></thead><tbody>{users.map(u => <tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.career?.acronym ?? "—"}</td><td><span className="status approved">{u.active ? "Activo" : "Inactivo"}</span></td></tr>)}</tbody></table></section></div></>; }
