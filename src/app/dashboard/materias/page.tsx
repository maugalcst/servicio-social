import { DashboardHeader } from "@/components/dashboard-header";
import { prisma } from "@/lib/prisma";
export default async function MateriasPage() { const rows = await prisma.subject.findMany({ include: { career: true }, orderBy: [{ semester: "asc" }, { name: "asc" }] }); return <><DashboardHeader title="Lista de materias" subtitle="Catálogo académico disponible." /><div className="content-wrap"><section className="table-card"><div className="table-heading"><div><h2>Materias</h2><p>Información por carrera y semestre</p></div></div><table><thead><tr><th>Clave</th><th>Materia</th><th>Carrera</th><th>Semestre</th></tr></thead><tbody>{rows.map(x => <tr key={x.id}><td>{x.code}</td><td>{x.name}</td><td><span className="career-pill">{x.career.acronym}</span></td><td>{x.semester}</td></tr>)}</tbody></table></section></div></>; }
